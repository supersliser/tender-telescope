---
const pages = Astro.glob("../projects/*.md")
const { slug } = Astro.params;
const page = pages.find((page) => page.frontmatter.slug === slug);
if (!page) return Astro.redirect("/404");
const { title, text, type } = page.frontmatter;
import Layout from "../layouts/MDLayout.astro"
import AnimBlock from "../components/AnimationBlock.astro";
---

<Layout>

{if (type == "render"){



}}

</Layout>